import { styled } from "@mui/material/styles";
import MuiButton from "@mui/material/Button";
export const Button = styled(MuiButton)(({ key }) => ({
    width: 100,
    height: 100,
    backgroundColor: "darkseagreen",
}));